const PROD_CONFIG = 'prod_config';
const STG_CONFIG = 'staging_config';
const DEV_CONFIG = 'dev_config';

class AppStrings {
  AppStrings._();

  // for storing the primary color of the app
  static const primaryColorKey = 'primaryColorKey';
}
